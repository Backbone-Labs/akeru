# Initial CodeQL alert review

PR #20 retains CodeQL scanning and severity enforcement. These specific reports were reviewed against code and test boundaries; no query or directory was excluded. Re-review CLI findings before turning operator tools into an upload/network service.

- Alert 2 — false positive: Reviewed updateAvailabilityFile and its CLI caller: the operator intentionally chooses the local availability record path. Game messages and HTTP requests cannot call this filesystem helper. It only performs pause/retained rollback, uses exclusive temp/lock creation, and is documented as operating on an operator-owned record. This is not a remotely supplied path or a privilege boundary. Re-review before exposing it through a service.
- Alert 3 — false positive: Reviewed updateAvailabilityFile and its CLI caller: the operator intentionally chooses the local availability record path. Game messages and HTTP requests cannot call this filesystem helper. It only performs pause/retained rollback, uses exclusive temp/lock creation, and is documented as operating on an operator-owned record. This is not a remotely supplied path or a privilege boundary. Re-review before exposing it through a service.
- Alert 4 — false positive: Reviewed updateAvailabilityFile and its CLI caller: the operator intentionally chooses the local availability record path. Game messages and HTTP requests cannot call this filesystem helper. It only performs pause/retained rollback, uses exclusive temp/lock creation, and is documented as operating on an operator-owned record. This is not a remotely supplied path or a privilege boundary. Re-review before exposing it through a service.
- Alert 5 — false positive: Reviewed updateAvailabilityFile and its CLI caller: the operator intentionally chooses the local availability record path. Game messages and HTTP requests cannot call this filesystem helper. It only performs pause/retained rollback, uses exclusive temp/lock creation, and is documented as operating on an operator-owned record. This is not a remotely supplied path or a privilege boundary. Re-review before exposing it through a service.
- Alert 6 — false positive: Reviewed updateAvailabilityFile and its CLI caller: the operator intentionally chooses the local availability record path. Game messages and HTTP requests cannot call this filesystem helper. It only performs pause/retained rollback, uses exclusive temp/lock creation, and is documented as operating on an operator-owned record. This is not a remotely supplied path or a privilege boundary. Re-review before exposing it through a service.
- Alert 7 — false positive: Reviewed updateAvailabilityFile and its CLI caller: the operator intentionally chooses the local availability record path. Game messages and HTTP requests cannot call this filesystem helper. It only performs pause/retained rollback, uses exclusive temp/lock creation, and is documented as operating on an operator-owned record. This is not a remotely supplied path or a privilege boundary. Re-review before exposing it through a service.
- Alert 8 — false positive: Reviewed updateAvailabilityFile and its CLI caller: the operator intentionally chooses the local availability record path. Game messages and HTTP requests cannot call this filesystem helper. It only performs pause/retained rollback, uses exclusive temp/lock creation, and is documented as operating on an operator-owned record. This is not a remotely supplied path or a privilege boundary. Re-review before exposing it through a service.
- Alert 16 — false positive: Reviewed source-inventory CLI: OUTPUT.json is an explicit operator-selected output filename, written with flag wx. There is no remote caller, elevated service or title-controlled destination. Arbitrary local output selection is the documented CLI functionality, not traversal across a security boundary.
- Alert 18 — false positive: Reviewed rights-audit CLI: AUDIT_NAME must match names from the checked-in compliance/rights-audits/index.json before building the audit filename. The inventory name comes from that checked-in audit. These are trusted repository policy inputs, not upload/game input; unmatched names throw before reading.
- Alert 19 — false positive: Reviewed hosted verification CLI: the operator explicitly selects the HTTPS deployment origin to probe; no game or HTTP handler can select it. Exact URL.origin equality rejects credentials/path/query; redirects are errors. This intentional outbound deployment verification is not a server-side request-forgery boundary. Do not expose the helper as an untrusted URL-fetch service without network policy.
- Alert 20 — false positive: Reviewed hosted verification CLI: the operator explicitly selects the HTTPS deployment origin to probe; no game or HTTP handler can select it. Exact URL.origin equality rejects credentials/path/query; redirects are errors. This intentional outbound deployment verification is not a server-side request-forgery boundary. Do not expose the helper as an untrusted URL-fetch service without network policy.
- Alert 21 — used in tests: Reviewed examples/browser-conformance/title.js: these are intentional hostile fetch/script/image/iframe probes in the local isolated-title conformance harness. The test verifies CSP and origin isolation block them; removing the probes would remove security coverage. They are not part of the packaged production catalog. Collector traffic is a test assertion, not game functionality.
- Alert 22 — used in tests: Reviewed examples/browser-conformance/title.js: these are intentional hostile fetch/script/image/iframe probes in the local isolated-title conformance harness. The test verifies CSP and origin isolation block them; removing the probes would remove security coverage. They are not part of the packaged production catalog. Collector traffic is a test assertion, not game functionality.
- Alert 23 — used in tests: Reviewed examples/browser-conformance/title.js: these are intentional hostile fetch/script/image/iframe probes in the local isolated-title conformance harness. The test verifies CSP and origin isolation block them; removing the probes would remove security coverage. They are not part of the packaged production catalog. Collector traffic is a test assertion, not game functionality.
- Alert 24 — used in tests: Reviewed examples/browser-conformance/title.js: these are intentional hostile fetch/script/image/iframe probes in the local isolated-title conformance harness. The test verifies CSP and origin isolation block them; removing the probes would remove security coverage. They are not part of the packaged production catalog. Collector traffic is a test assertion, not game functionality.
- Alert 26 — used in tests: Reviewed examples/browser-conformance/title.js: these are intentional hostile fetch/script/image/iframe probes in the local isolated-title conformance harness. The test verifies CSP and origin isolation block them; removing the probes would remove security coverage. They are not part of the packaged production catalog. Collector traffic is a test assertion, not game functionality.
- Alert 27 — false positive: Reviewed app.js onMessage -> active.channel.receive. platform/catalog/channel.js receive rejects event.source !== frame OR event.origin !== origin before reading event.data, then checks nonce, protocol, monotonic sequence and payload. Node tests and Chromium forged-message test cover rejection. The origin check is downstream of the listener, not missing.
- Alert 28 — used in tests: Reviewed tests/compliance.test.mjs: synthetic dossier property assignment iterates checked-in clearance policy section names while constructing negative/positive test fixtures. No external user property enters production objects. The test asserts missing or altered rights evidence cannot grant publication.
- Alert 33 — false positive: Reviewed hosted verification health comparisons: remote reported revision/digest/state are compared against the locally expected release, with any mismatch throwing. This is intended verification of untrusted response data, not an attacker-controllable switch that disables a security check. Asset hashes and headers are subsequently checked independently.
- Alert 34 — false positive: Reviewed hosted verification health comparisons: remote reported revision/digest/state are compared against the locally expected release, with any mismatch throwing. This is intended verification of untrusted response data, not an attacker-controllable switch that disables a security check. Asset hashes and headers are subsequently checked independently.
- Alert 35 — false positive: Reviewed rights-audit CLI name guard: names.includes(name) is the allowlist check against checked-in policy and throws for unmatched operator input. User input must affect the rejection condition; this does not bypass it.
- Alert 36 — false positive: Reviewed optional rights source-checkout mode: omission skips byte verification but output explicitly reports sourceBytesVerified:false and approvalStatus:pending. Neither mode grants publication approval. This documented evidence-only mode is not a bypass of the separate publication gate.
- Alert 37 — false positive: Reviewed hosted catalog verification: only validated release-relative asset filenames are appended to the operator-chosen deployment origin for GET requests. Local file contents are never sent. CLI loadCatalog validates release paths and hashes before use. Downloaded remote bytes are compared with expected local hashes.

## Findings addressed in code

- Resource amplification: manifest byte/structural limits, bounded arrays, fail-fast schema validation, linear path coverage, streamed bounded artifact hashing.
- Local file races and evidence path escape: descriptor-based regular-file reads, symlink containment checks, and nonblocking rejection of FIFO inputs. Tests cover outside-root symlinks and tracked directory replacement. Portable Node does not isolate an operator workspace from a hostile concurrent process replacing ancestor directories; these tools remain offline tools, not an upload sandbox.

These changes are validated separately from the reviewed false positives. GitHub scan results determine which findings are fixed in the PR; default-branch findings may remain open until merge.
